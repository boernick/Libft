#include "libft.h"
#include <string.h>
#include <stdio.h>
#include <limits.h>

int main() 
{

}
	// char	**s;
	// char	*d;
	// char	**s1;

	// s = ft_split("ik ben nick", 'c');
	// s1 = s;
	// while (*s)
	// {
	// 	d = *s;
	// 	printf("array: %s\n", *s++);
	// 	free(d);
	// }
	// free(s1);
